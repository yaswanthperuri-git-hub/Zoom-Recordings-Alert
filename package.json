{
  "name": "zoom-slack-webhook",
  "version": "1.0.0",
  "description": "Sends a Slack alert when a Zoom recording is deleted",
  "main": "api/zoom-webhook.js",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
